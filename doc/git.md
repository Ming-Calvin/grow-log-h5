```
# 确保在仓库根目录并且工作目录干净
git status

# 创建并切换到 initProject 分支
git checkout -b initProject

# 确认已经切换到 initProject 分支
git branch

# 推送 initProject 分支到远程
git push origin initProject

# 切换回 main 分支（如果需要）
git checkout main

# 合并 initProject 分支到 main 分支（如果需要）
git merge initProject

# 推送 main 分支的更改到远程（如果需要）
git push origin main

```

